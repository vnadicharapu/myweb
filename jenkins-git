pipeline{
    agent any
    environment {
         PATH = "/usr/share/apache-maven:$PATH"
    }
    stages{
        stage("maven-build"){
            steps{
                sh "mvn clean package"
            }
        }
    }
}
