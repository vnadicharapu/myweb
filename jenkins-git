pipeline{
    agent any
    tools {
        maven 'apache-maven-1.0.1' 
    }
    stages{
        stage("maven-build"){
            steps{
                sh "mvn clean package"
            }
        }
    }
}
